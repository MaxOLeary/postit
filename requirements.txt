# Postit dependencies. Install with:  pip install -r requirements.txt
#
# PySide6 = the Qt GUI toolkit (the whole widget).
PySide6>=6.10

# pyobjc = the bridge to native macOS. Only needed for the frosted "liquid
# glass" blur (apply_liquid_glass); the app still runs without it, just flat.
# pyobjc-framework-Cocoa pulls in AppKit (NSVisualEffectView) and pyobjc-core.
pyobjc-framework-Cocoa>=11.0 ; sys_platform == "darwin"
